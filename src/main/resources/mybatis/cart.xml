<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "dev.mvc.cart.CartDAOInter">

  <insert id="create" parameterType="dev.mvc.cart.CartVO">
    INSERT INTO cart(cartno, movieno, memberno, rdate)
    VALUES (cart_seq.nextval,#{movieno},#{memberno}, sysdate)
  </insert>

  <!--개인 장바구니 리스트 조회-->
  <select id="cartList" resultType="dev.mvc.cart.CartVO">
    SELECT cartno, movieno, rdate
    FROM cart
    WHERE memberno = #{memberno}
  </select>

  <!--개인 장바구니 내 영화 조회-->
  <select id="cartMovieDetail" resultType="dev.mvc.cart.CartVO" parameterType="int">
    SELECT mno, title, director, act, plot
    FROM movie
    WHERE mno = #{movieno}
  </select>

  <delete id="delete" parameterType="int">
    DELETE FROM cart
    WHERE cartno=#{cartno}
  </delete>


</mapper>